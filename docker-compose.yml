version: '3.8'
services:
  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: ivan
      POSTGRES_PASSWORD: superpassword123
      POSTGRES_DB: gamer_vault_db
    ports:
      - '5432:5432'